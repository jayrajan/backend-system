[project]
name = "backend-app"
version = "0.1.0"
description = "A simple backend application for an e-commerce system"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.128.8",
]
